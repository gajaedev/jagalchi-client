'use client';

import ProfileHeader from '@/features/profile/components/molecules/ProfileHeader';

export default function Profile() {
  return <div className="flex w-[950px] items-center justify-center">{ProfileHeader()}</div>;
}

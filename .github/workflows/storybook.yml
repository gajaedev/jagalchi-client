# ⚠️ GitHub Pages 미활성화 상태
# Pages 활성화 후 사용 가능: Settings > Pages > Source: gh-pages branch
name: Storybook

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build Storybook
        run: pnpm build-storybook

      - name: Add vercel.json to disable Vercel deployments from gh-pages
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop'
        run: echo '{"git":{"deploymentEnabled":false}}' > ./storybook-static/vercel.json

      - name: Deploy to GitHub Pages
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop'
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./storybook-static